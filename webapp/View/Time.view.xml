<mvc:View
	controllerName="sap.ui.agi.zeiterfassung.controller.Time"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:unified="sap.ui.unified">
	<VBox class="sapUiSmallMargin">
		<SinglePlanningCalendar
			id="SPC1"
			class="sapUiSmallMarginTop"
			title= "{i18n>TimeTitle}"
			startDate="{path: '/startDate'}"
			appointments="{path: '/appointments'}">
			<actions>
				<Button
					id="addNewAppointment"
					text="{i18n>TimeCreateButtonText}"
					press="handleAppointmentCreate"
					tooltip="{i18n>TimeCreateButtonTooltip}" />
			</actions>
			<views>
				<SinglePlanningCalendarDayView key="DayView" title="Day" />
				<SinglePlanningCalendarWorkWeekView key="WorkWeekView" title="Work Week" />
				<SinglePlanningCalendarWeekView key="WeekView" title="Week" />
				<SinglePlanningCalendarMonthView key="MonthView" title="Month" />
			</views>
			<appointments>
				<unified:CalendarAppointment
					title= "{title}"
					text= "{text}"
					type= "{type}"
					icon= "{icon}"
					startDate= "{startDate}"
					endDate= "{endDate}">
				</unified:CalendarAppointment>
			</appointments>
		</SinglePlanningCalendar>
	</VBox>
</mvc:View>