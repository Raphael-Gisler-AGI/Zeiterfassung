<mvc:View
	controllerName="sap.ui.agi.zeiterfassung.controller.Time"
	xmlns:f="sap.f"
	xmlns:cssgrid="sap.ui.layout.cssgrid"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns:grid="sap.ui.layout.cssgrid"
	xmlns="sap.m">
	<grid:CSSGrid gridGap="50px">
		<FlexBox alignItems="Center" width="100%" justifyContent="SpaceBetween">
			<Input
				value="{Timer>/Description}"
				placeholder="Description"
				width="400px" />
			<cssgrid:CSSGrid gridTemplateColumns="1fr 1fr 1fr" gridGap="20px">
				<NumericContent value="{Timer>/time}" />
				<Button
					press="onPressActivate"
					text="{= ${Timer>/active} ? 'Stop' : 'Start'}" />
				<Button
					press="onPressSave"
					text="Save" />
			</cssgrid:CSSGrid>
		</FlexBox>
		<ScrollContainer height="100vh" vertical="true">
			<Table id="idProductsTable"
				inset="false"
				items="{
				path: 'entries>/',
				sorter: {
					path: 'Day',
					group: true
					}
				}">
				<headerToolbar>
					<OverflowToolbar>
						<content>
							<Title text="Products" level="H2" />
							<ToolbarSpacer />
							<ComboBox id="idPopinLayout"
								placeholder="Popin layout options"
								change="onPopinLayoutChanged">
								<items>
									<core:Item text="Block"
										key="Block" />
									<core:Item text="Grid Large"
										key="GridLarge" />
									<core:Item text="Grid Small"
										key="GridSmall" />
								</items>
							</ComboBox>
						</content>
					</OverflowToolbar>
				</headerToolbar>
				<columns>
					<Column
						width="12em">
						<Text text="Description" />
					</Column>
					<Column
						demandPopin="true">
						<Text text="Time" />
					</Column>
					<Column>
						<Text text="Duration" />
					</Column>
					<Column
						width="80px"
						hAlign="Right">
						<Text text="Edit" />
					</Column>
					<Column
						width="80px"
						hAlign="Right">
						<Text text="Delete" />
					</Column>
				</columns>
				<items>
					<ColumnListItem vAlign="Middle">
						<cells>
							<ObjectIdentifier
								title="{entries>Description}" />
							<HBox>
								<Text
									text="{
								path: 'entries>StartTime',
								type: 'sap.ui.model.type.Date',
								formatOptions: {pattern: 'hh:mm:ss'}}-" />
								<Text
									text="{
								path: 'entries>EndTime',
								type: 'sap.ui.model.type.Date',
								formatOptions: {pattern: 'hh:mm:ss'}}" />
							</HBox>
							<Text
								text="{entries>Duration}" />
							<Button text="Edit" />
							<Button
								press="onPressDelete"
								text="Delete" />
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</ScrollContainer>
	</grid:CSSGrid>
</mvc:View>